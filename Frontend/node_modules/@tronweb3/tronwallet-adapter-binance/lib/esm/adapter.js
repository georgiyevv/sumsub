var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { Adapter, AdapterState, isInBrowser, WalletReadyState, WalletSignMessageError, WalletNotFoundError, WalletDisconnectedError, WalletConnectionError, WalletSignTransactionError, WalletGetNetworkError, NetworkType, } from '@tronweb3/tronwallet-abstract-adapter';
import { WalletConnectAdapter, } from '@tronweb3/tronwallet-adapter-walletconnect';
export const BinanceWalletAdapterName = 'Binance Wallet';
const chainIdNetworkMap = {
    '0x2b6653dc': NetworkType.Mainnet,
    '0x94a9059e': NetworkType.Shasta,
    '0xcd8690dc': NetworkType.Nile,
};
export class BinanceWalletAdapter extends Adapter {
    constructor(config = {}) {
        super();
        this.name = BinanceWalletAdapterName;
        this.url = 'https://www.binance.com/en/binancewallet';
        this.icon = 'data:image/svg+xml;base64,';
        this._readyState = isInBrowser() ? WalletReadyState.Loading : WalletReadyState.NotFound;
        this._state = AdapterState.Loading;
        this._walletConnectAdapter = null;
        this._wcDisconnectHandler = null;
        this._onAccountsChanged = (address) => {
            const preAddr = this.address || '';
            this.setAddress(Array.isArray(address) ? address[0] : address);
            this.emit('accountsChanged', this.address || '', preAddr);
        };
        this._checkPromise = null;
        this._updateProvider = () => {
            var _a;
            let state = this.state;
            let address = this.address;
            if ((_a = window.binancew3w) === null || _a === void 0 ? void 0 : _a.tron) {
                this._provider = window.binancew3w.tron;
                address = null; // Will be set when connected
                state = AdapterState.Disconnect;
            }
            else {
                this._provider = null;
                address = null;
                state = AdapterState.NotFound;
            }
            this.setAddress(address);
            this.setState(state);
        };
        const { checkTimeout = 2 * 1000, openUrlWhenWalletNotFound = true, useWalletConnectWhenWalletNotFound = false, walletConnectConfig, onWalletConnectUri, } = config;
        if (typeof checkTimeout !== 'number') {
            throw new Error('[BinanceWalletAdapter] config.checkTimeout should be a number');
        }
        this.config = {
            checkTimeout,
            openUrlWhenWalletNotFound,
            useWalletConnectWhenWalletNotFound,
            walletConnectConfig,
            onWalletConnectUri,
        };
        this._connecting = false;
        this._provider = null;
        this._address = null;
        if (!isInBrowser()) {
            this._readyState = WalletReadyState.NotFound;
            this.setState(AdapterState.NotFound);
            return;
        }
        this._checkWallet().then(() => {
            if (this.connected) {
                this.emit('connect', this.address || '');
                this._listenEvent();
            }
        });
    }
    get address() {
        return this._address;
    }
    get state() {
        return this._state;
    }
    get readyState() {
        return this._readyState;
    }
    get connecting() {
        return this._connecting;
    }
    /**
     * Set the onWalletConnectUri callback for custom QR code rendering.
     * This allows dynamic configuration of the URI handler after adapter initialization.
     *
     * Note: If called while connecting, the change will not affect the current connection
     * but will take effect on the next connection attempt.
     *
     * @param callback - Callback to receive the WalletConnect URI
     */
    setOnWalletConnectUri(callback) {
        if (this._connecting) {
            console.warn('[BinanceWalletAdapter] Changing onWalletConnectUri callback while connecting will take effect on next connection');
        }
        this.config.onWalletConnectUri = callback;
    }
    /**
     * Get network information used by Binance Wallet.
     * @returns {Network} Current network information.
     */
    network() {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            try {
                yield this._checkWallet();
                if (this.state !== AdapterState.Connected)
                    throw new WalletDisconnectedError();
                // If using WalletConnect fallback, delegate to WalletConnect adapter
                if (this._walletConnectAdapter && !this._provider) {
                    // WalletConnect doesn't expose network() method, return default network from config
                    const networkType = (_a = this.config.walletConnectConfig) === null || _a === void 0 ? void 0 : _a.network;
                    const chainIdMap = {
                        Mainnet: '0x2b6653dc',
                        Shasta: '0x94a9059e',
                        Nile: '0xcd8690dc',
                    };
                    return {
                        networkType: chainIdNetworkMap[chainIdMap[networkType] || ''] || NetworkType.Unknown,
                        chainId: chainIdMap[networkType] || '',
                        fullNode: '',
                        solidityNode: '',
                        eventServer: '',
                    };
                }
                try {
                    const chainId = this._provider.getChainId();
                    return {
                        networkType: chainIdNetworkMap[chainId] || NetworkType.Unknown,
                        chainId,
                        fullNode: '',
                        solidityNode: '',
                        eventServer: '',
                    };
                }
                catch (e) {
                    throw new WalletGetNetworkError(e === null || e === void 0 ? void 0 : e.message, e);
                }
            }
            catch (e) {
                this.emit('error', e);
                throw e;
            }
        });
    }
    connect() {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (this.connected || this.connecting)
                    return;
                yield this._checkWallet();
                this._connecting = true;
                // Check if we should use WalletConnect fallback
                // Either state is NotFound, or we previously used WalletConnect (provider is null)
                const shouldUseWalletConnect = this.state === AdapterState.NotFound || !this._provider;
                if (shouldUseWalletConnect) {
                    if (!this.config.useWalletConnectWhenWalletNotFound) {
                        if (this.config.openUrlWhenWalletNotFound !== false && isInBrowser()) {
                            window.open(this.url, '_blank');
                        }
                        throw new WalletNotFoundError();
                    }
                    // Use WalletConnect as fallback
                    if (!this.config.walletConnectConfig) {
                        throw new WalletConnectionError('[BinanceWalletAdapter] walletConnectConfig is required when useWalletConnectWhenWalletNotFound is true');
                    }
                    // Reuse existing WalletConnect adapter if available
                    if (!this._walletConnectAdapter) {
                        this._walletConnectAdapter = new WalletConnectAdapter(this.config.walletConnectConfig);
                    }
                    try {
                        // Pass onWalletConnectUri option from config to WalletConnect
                        const wcOptions = this.config.onWalletConnectUri
                            ? {
                                onUri: this.config.onWalletConnectUri,
                            }
                            : undefined;
                        yield this._walletConnectAdapter.connect(wcOptions);
                        this.setAddress(this._walletConnectAdapter.address);
                        this.setState(AdapterState.Connected);
                        this.emit('connect', this._walletConnectAdapter.address);
                        // Listen to WalletConnect events (only if not already listening)
                        if (!this._wcDisconnectHandler) {
                            this._walletConnectAdapter.on('accountsChanged', this._onAccountsChanged);
                            this._wcDisconnectHandler = () => {
                                this.setAddress(null);
                                this.setState(AdapterState.Disconnect);
                                this.emit('disconnect');
                            };
                            this._walletConnectAdapter.on('disconnect', this._wcDisconnectHandler);
                        }
                    }
                    catch (error) {
                        // Don't clear the adapter instance on error, keep it for retry
                        throw new WalletConnectionError(error === null || error === void 0 ? void 0 : error.message, error);
                    }
                    return;
                }
                try {
                    const { address } = yield this._provider.getAccount();
                    this.setAddress(address);
                    this.setState(AdapterState.Connected);
                    this.emit('connect', address);
                    this._listenEvent();
                }
                catch (error) {
                    throw new WalletConnectionError(error === null || error === void 0 ? void 0 : error.message, error);
                }
            }
            catch (error) {
                this.emit('error', error);
                throw error;
            }
            finally {
                this._connecting = false;
            }
        });
    }
    disconnect() {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.state !== AdapterState.Connected) {
                return;
            }
            // Disconnect WalletConnect if used
            if (this._walletConnectAdapter) {
                this._walletConnectAdapter.off('accountsChanged', this._onAccountsChanged);
                if (this._wcDisconnectHandler) {
                    this._walletConnectAdapter.off('disconnect', this._wcDisconnectHandler);
                    this._wcDisconnectHandler = null;
                }
                yield this._walletConnectAdapter.disconnect();
                // Keep the adapter instance for reuse, don't set to null
            }
            else {
                yield this._provider.disconnect();
                this._stopListenEvent();
            }
            this.setAddress(null);
            this.setState(AdapterState.Disconnect);
            this.emit('disconnect');
        });
    }
    signMessage(message) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (this.state !== AdapterState.Connected)
                    throw new WalletDisconnectedError();
                // Use WalletConnect if connected via WalletConnect
                if (this._walletConnectAdapter) {
                    return yield this._walletConnectAdapter.signMessage(message);
                }
                try {
                    return yield this._provider.signMessageV2(message);
                }
                catch (error) {
                    throw new WalletSignMessageError(error === null || error === void 0 ? void 0 : error.message, error);
                }
            }
            catch (error) {
                this.emit('error', error);
                throw error;
            }
        });
    }
    signTransaction(transaction) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (this.state !== AdapterState.Connected)
                    throw new WalletDisconnectedError();
                // Use WalletConnect if connected via WalletConnect
                if (this._walletConnectAdapter) {
                    return yield this._walletConnectAdapter.signTransaction(transaction);
                }
                try {
                    return yield this._provider.signTransaction(transaction);
                }
                catch (error) {
                    throw new WalletSignTransactionError(error === null || error === void 0 ? void 0 : error.message, error);
                }
            }
            catch (error) {
                this.emit('error', error);
                throw error;
            }
        });
    }
    /**
     * Sign transaction and broadcast the signed transaction with Binance wallet selected network.
     * @param transaction
     * @returns
     */
    signAndSendTransaction(transaction) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (!this.connected)
                    throw new WalletDisconnectedError();
                try {
                    const res = yield this._provider.signAndSendTransaction(transaction);
                    if (typeof res.transaction === 'string') {
                        try {
                            res.transaction = JSON.parse(res.transaction);
                        }
                        catch (e) {
                            console.error(`[BinanceWalletAdapter] parse transaction error: ${e}`);
                        }
                    }
                    return res;
                }
                catch (error) {
                    throw new WalletSignTransactionError(error === null || error === void 0 ? void 0 : error.message, error);
                }
            }
            catch (error) {
                this.emit('error', error);
                throw error;
            }
        });
    }
    _listenEvent() {
        this._stopListenEvent();
        this._provider.on('accountsChanged', this._onAccountsChanged);
    }
    _stopListenEvent() {
        this._provider.removeListener('accountsChanged', this._onAccountsChanged);
    }
    _checkWallet() {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.readyState === WalletReadyState.Found) {
                return true;
            }
            if (this._checkPromise) {
                return this._checkPromise;
            }
            const interval = 100;
            const maxTimes = Math.floor(this.config.checkTimeout / interval);
            let times = 0;
            let timer;
            this._checkPromise = new Promise((resolve) => {
                const check = () => {
                    var _a;
                    times++;
                    const isSupport = !!((_a = window.binancew3w) === null || _a === void 0 ? void 0 : _a.tron);
                    if (isSupport || times > maxTimes) {
                        timer && clearInterval(timer);
                        this._readyState = isSupport ? WalletReadyState.Found : WalletReadyState.NotFound;
                        this._updateProvider();
                        this.emit('readyStateChanged', this.readyState);
                        resolve(isSupport);
                    }
                };
                timer = setInterval(check, interval);
                check();
            });
            return this._checkPromise;
        });
    }
    setAddress(address) {
        this._address = address;
    }
    setState(state) {
        const preState = this.state;
        if (state !== preState) {
            this._state = state;
            this.emit('stateChanged', state);
        }
    }
}
//# sourceMappingURL=adapter.js.map